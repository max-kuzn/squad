import json
from constants import TOKENIZED_TRAIN_PATH, EMBEDDED_TRAIN_PATH
from constants import EMBEDDING_PATH

def main():
    tokenized_data = json.load(open(TOKENIZED_TRAIN_PATH, 'r'))

if __name__ == "__main__":
    main()

